# Smithery configuration file: https://smithery.ai/docs/config#smitheryyaml

startCommand:
  type: stdio
  configSchema:
    # JSON Schema defining the configuration options for the MCP
    type: object
    required:
      - podbeanClientId
      - podbeanClientSecret
    properties:
      podbeanClientId:
        type: string
        description: "The Client ID for the Podbean API"
      podbeanClientSecret:
        type: string
        description: "The Client Secret for the Podbean API"
  commandFunction: |
    function(config) {
      return [
        "python",
        "-c",
        `
import os
import sys
import subprocess

# Set environment variables from config
os.environ["PODBEAN_CLIENT_ID"] = "${config.podbeanClientId}"
os.environ["PODBEAN_CLIENT_SECRET"] = "${config.podbeanClientSecret}"

# Run the server
subprocess.run([sys.executable, "server.py"])
        `
      ];
    }

metadata:
  name: "Podbean MCP"
  description: "An MCP server for managing your podcast through the Podbean API"
  version: "0.1.0"
  author: "Podbean MCP Team"
  tags:
    - podcast
    - podbean
    - audio
    - media
    - content
  
documentation:
  overview: |
    # 🎙️ Podbean MCP Server 🎧

    This MCP server connects any MCP-compatible AI assistant to the Podbean API. Whether you're using Cline (the IDE MCP Client), Claude Desktop, or any other MCP client, you can now manage your podcasts, episodes, and analytics through natural conversation!

  configuration: |
    ## Configuration

    To use this MCP server, you need to provide your Podbean API credentials:

    - `podbeanClientId`: Your Podbean API Client ID
    - `podbeanClientSecret`: Your Podbean API Client Secret

    You can obtain these credentials from your Podbean account settings.

  features: |
    ## ✨ Features

    ### 🔐 Authentication
    - Client credentials authentication for managing your own podcasts
    - OAuth flow for third-party access (when needed)
    - Token management for multiple podcasts

    ### 🎙️ Podcast Management
    - List all your podcasts in one place
    - Get detailed information about your shows
    - View stats and analytics
    - Browse podcast categories

    ### 📝 Episode Management
    - See all episodes for your podcast
    - Get details about specific episodes
    - Publish new episodes
    - Update existing episodes
    - Delete episodes

    ### 📊 Analytics
    - Check download statistics
    - Track daily listener counts
    - Monitor user engagement

    ### 🌐 Public Podcast Access
    - Access public podcast data through oEmbed
    - Get information on any public episode

  examples: |
    ## 💬 Example Usage

    Here are some ways to use this MCP server:

    1. **Authenticate with Podbean**:
       ```
       Can you authenticate with my Podbean account?
       ```

    2. **List your podcasts**:
       ```
       Show me all my podcasts
       ```

    3. **Get episodes for a podcast**:
       ```
       List all episodes from my podcast with ID "your_podcast_id"
       ```

    4. **Publish a new episode**:
       ```
       I want to publish a new episode called "AI in Podcasting" about how AI is making podcasting easier
       ```

    5. **Check podcast statistics**:
       ```
       How's my podcast doing? Show me the download stats from last week
